export type CountryData = {
  code: string;
  name: string;
  flag: string;
  flagUrl: string;
  dialCode: string;
  digits: number;
  currency: string;
  cities: string[];
};

export type CurrencyData = {
  code: string;
  label: string;
  flag: string;
  flagUrl: string;
};
